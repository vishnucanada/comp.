# Core has zero runtime dependencies — install with `pip install -e .`
# This file is for the dashboard + dev workflow.

# Dashboard
fastapi>=0.100
uvicorn>=0.23
pydantic>=2.0
anthropic>=0.25

# Optional guard backends — install only the one(s) you need.
# openai>=1.0
# torch>=2.0
# transformers>=4.40

# YAML IAM configs
pyyaml>=6.0

# Dev / test
pytest>=8.0
